#include "array.h"
#include "dtype.h"

namespace np {
    template<typename _Tp>
    np::Array& empty(std::tuple<_Tp> shape,
                    np::dtype type, 
                    np::matrix::Order order,
                    std::optional<std::string> device,
                    std::optional<np::Array> like)
    {

        
    }











}